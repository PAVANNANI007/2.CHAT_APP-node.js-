{
  "name": "nodeserver",
  "version": "1.0.0",
  "description": "this is nodeserver for chat app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "PAVAN",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^2.0.7",
    "socket.io": "^3.1.1"
  }
}
